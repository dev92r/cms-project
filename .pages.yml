content:
  - name: features
    label: Features
    type: collection
    path: content/homepage/features
    format: json

    fields:
      - name: title
        label: Title
        type: string
        required: true

      - name: description
        label: Description
        type: rich-text
        required: true